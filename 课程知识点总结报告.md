# 计算理论导论 (CS338) - 完整知识点总结报告

**课程**: Introduction to Theory of Computation  
**学校**: Southern University of Science and Technology (SUSTech)  
**学期**: 2026 Spring  
**作者**: 基于 Yanqiao Chen 的 Lab Answers 整理

---

## 目录

1. [Week 1: 确定性有限自动机 (DFA)](#week-1)
2. [Week 2: 非确定性有限自动机 (NFA)](#week-2)
3. [Week 3: 正则表达式](#week-3)
4. [Week 4: 泵引理与非正则语言](#week-4)
5. [Week 5: 上下文无关文法 (CFG)](#week-5)
6. [Week 6: 下推自动机 (PDA)](#week-6)
7. [Week 7: 图灵机 (Turing Machine)](#week-7)
8. [Week 8: 可判定性 (Decidability)](#week-8)
9. [Week 9: 可计算性基础](#week-9)
10. [Week 10: 归约与复杂性入门](#week-10)
11. [Week 11: NP 类问题](#week-11)
12. [Week 12: NP 完全性](#week-12)
13. [Week 13: Cook-Levin 定理与更多归约](#week-13)

---

## Week 1: 确定性有限自动机 (DFA) {#week-1}

### 核心定义

**定义 1.1 (DFA 形式化定义)**: 一个确定性有限自动机 $M$ 是一个五元组 $(Q, \Sigma, \delta, q_0, F)$，其中：
- $Q$ 是有限状态集
- $\Sigma$ 是有限字母表
- $\delta: Q \times \Sigma \to Q$ 是转移函数
- $q_0 \in Q$ 是初始状态
- $F \subseteq Q$ 是接受状态集

**定义 1.2 (接受字符串)**: DFA $M$ 接受字符串 $w = w_1 w_2 \ldots w_n$ 当且仅当存在状态序列 $r_0, r_1, \ldots, r_n$ 使得 $r_0 = q_0$，$r_{i+1} = \delta(r_i, w_{i+1})$，且 $r_n \in F$。

### Lab 问题

**Lab 1 - Question 1**: 给定两个DFA的状态图，识别：
- 状态集 $Q_1, Q_2$
- 接受状态集 $F_1, F_2$
- 特定输入串的状态转移路径
- 判断字符串是否被接受

**Lab 1 - Question 2**: 将状态图转换为形式化定义（表格表示转移函数）

**Lab 1 - Question 3-4**: 构造识别特定语言的DFA：
- 以 `00` 结尾的字符串
- 包含子串 `0101` 的字符串
- 包含偶数个0或恰好两个1的字符串
- 不包含子串 `ba` 的字符串
- 既不包含 `ab` 也不包含 `ba` 的字符串

**Lab 1 - Question 5**: 利用补集构造法：先构造识别"简化语言"的DFA，再交换接受/非接受状态

**Lab 1 - Question 6**: 利用乘积构造（Product Construction）实现交集：
- 构造识别"至少两个a且至少三个b"的DFA

---

## Week 2: 非确定性有限自动机 (NFA) {#week-2}

### 核心定义

**定义 2.1 (NFA)**: NFA 与 DFA 类似，但转移函数为 $\delta: Q \times (\Sigma \cup \{\varepsilon\}) \to \mathcal{P}(Q)$，允许：
- 同一符号转移到多个状态
- $\varepsilon$-转移（不消耗输入）

**定理 2.1 (NFA 与 DFA 等价)**: 每个 NFA 都有一个等价的 DFA，通过**子集构造法**（Subset Construction）转换。

**定义 2.2 ($\varepsilon$-闭包)**: 状态 $q$ 的 $\varepsilon$-闭包 $E(q)$ 是从 $q$ 出发仅通过 $\varepsilon$-转移可达的所有状态集合。

### Lab 问题

**Lab 2 - Question 1**: 构造识别以下语言的 NFA：
- $\{w \mid w \text{ ends with } 00\}$
- $\{w \mid w \text{ contains the substring } 0101\}$
- $\{w \mid w \text{ contains an even number of 0\'s, or exactly two 1\'s}\}$（使用 $\varepsilon$-分支）
- $\{w \mid w \text{ does not contain the substring } 111\}$
- 语言 $\{0\}$, $0^*1^*0^+$, $1^*(001^+)^*$, $\{\varepsilon\}$, $\{0^*\}$

**Lab 2 - Question 2**: 构造两个 NFA 的并集（Union）

**Lab 2 - Question 3**: 构造 NFA 的交集（通过 $\varepsilon$-连接两个自动机）

**Lab 2 - Question 4**: 构造 NFA 的 Kleene Star 闭包

**Lab 2 - Question 5**: 证明任何 NFA 都可以转换为只有一个接受状态的等价 NFA

**Lab 2 - Question 6**: 子集构造法示例：将给定 NFA 转换为 DFA

---

## Week 3: 正则表达式 {#week-3}

### 核心定义

**定义 3.1 (正则表达式)**: 正则表达式 $R$ 递归定义如下：
1. $a \in \Sigma$，$\varepsilon$，$\emptyset$ 是正则表达式
2. 若 $R_1, R_2$ 是正则表达式，则 $R_1 \cup R_2$、$R_1 R_2$、$R_1^*$ 也是
3. 无其他情况

**定理 3.1 (Kleene 定理)**: 一个语言是正则的，当且仅当它可以被某个正则表达式描述。

**构造方法**:
- **Thompson 构造法**: 将正则表达式转换为 NFA
- **状态消除法** (State Elimination): 将 NFA/DFA 转换为正则表达式

### Lab 问题

**Lab 3 - Question 1**: 写出描述以下语言的正则表达式：
- 以1开头、以0结尾的字符串：$1\Sigma^*0$
- 包含至少三个1的字符串：$\Sigma^*1\Sigma^*1\Sigma^*1\Sigma^*$
- 包含子串0101的字符串：$\Sigma^*0101\Sigma^*$
- 第3个符号为0的字符串：$\Sigma^2 0\Sigma^*$
- 长度为奇数的字符串
- 不以11结尾的字符串
- 长度不超过5的字符串：$(\Sigma \cup \varepsilon)^5$
- 长度至少为3且第3个符号为0的字符串
- 以1开始和结尾、或长度为1的字符串：$(1\Sigma)^*(1 \cup \varepsilon)$

**Lab 3 - Question 2**: Thompson 构造法示例：
- 为 $(((11)^*(00)) \cup 01)^*$ 构造 NFA

**Lab 3 - Question 3**: 状态消除法：
- 将给定 NFA 转换为正则表达式
- 步骤：添加新的起始/接受状态，逐步消除中间状态

**Lab 3 - Question 4**: 为注释语言 $C$ 构造 DFA 和正则表达式：
- 以 `/#` 开始，以 `#/` 结束，中间不包含 `#/`
- 正则表达式：$\/\#((a\cup b\cup /)^*\#^+(a\cup b))^*(a\cup b\cup /)^*\#^+\/$

---

## Week 4: 泵引理与非正则语言 {#week-4}

### 核心定理

**定理 4.1 (正则语言泵引理)**: 若 $L$ 是正则语言，则存在泵长度 $p$，使得对任何 $|w| \geq p$ 的 $w \in L$，可分解为 $w = xyz$ 满足：
1. $|xy| \leq p$
2. $|y| > 0$
3. $\forall i \geq 0,\ xy^i z \in L$

**应用**：证明语言**不是**正则的。

### Lab 问题

**Lab 4 - Question 1**: 使用泵引理证明以下语言非正则：
- $\{1^n 2^n 3^n \mid n \geq 0\}$：取 $w = 1^p 2^p 3^p$，泵 $y$ 在1的段中
- $\{(a^p b^p)^3 \mid p \geq 0\}$：取 $w = (a^p b^p)^3$
- $\{a^{2^n} \mid n \geq 0\}$：取 $w = a^{2^p}$，证明 $2^p < 2^p + l < 2^{p+1}$

**Lab 4 - Question 2**: 分析为什么 $0^p 1^p$ 不能用于证明 $0^*1^*$ 非正则

**Lab 4 - Question 3**: 更多泵引理应用：
- $\{0^m 1^m 0^m \mid m \geq 0\}$ 非正则
- 利用闭包性质证明 $\{0^m 1^n \mid m \neq n\}$ 非正则：
  - 假设其正则，则其与 $0^*1^*$ 的补集交 $\{0^k 1^k\}$ 也应正则，矛盾

**Lab 4 - Question 4**: 证明正则性：
- "01出现次数等于10出现次数" 的语言是正则的
- 等价条件：$w \in \{\varepsilon, 0, 1\}$ 或首尾字符相同
- 正则表达式：$\{\varepsilon, 0, 1\} \cup 0\Sigma^*0 \cup 1\Sigma^*1$

**Lab 4 - Question 5**: 分析复杂语言：
- 语言 $B = \{1w \mid w \text{ 至少包含一个 } 1\}$ 是正则的
- 语言 $C = \{1^k y \mid y \text{ 中1的个数 } \leq k\}$ 不是正则的

---

## Week 5: 上下文无关文法 (CFG) {#week-5}

### 核心定义

**定义 5.1 (CFG)**: 上下文无关文法 $G = (V, \Sigma, R, S)$，其中：
- $V$ 是变元（变量）有限集
- $\Sigma$ 是终结符有限集
- $R$ 是规则（产生式）集，形式为 $A \to w$，$A \in V$，$w \in (V \cup \Sigma)^*$
- $S \in V$ 是起始变元

**定义 5.2 (派生/推导)**: 从起始变元 $S$ 开始，反复应用产生式规则，最终得到终结符串。

**定义 5.3 (歧义性)**: 若一个字符串有两棵不同的语法分析树，则该文法是歧义的。

**定义 5.4 (Chomsky Normal Form, CNF)**: 所有产生式形式为 $A \to BC$ 或 $A \to a$。

### Lab 问题

**Lab 5 - Question 1**: 为表达式 $a$, $a+a$, $a+a+a$（$(a+a)+a$）, $((a))$ 绘制语法分析树

**Lab 5 - Question 2**: 分析给定CFG的性质：
- 变元、终结符、起始变元
- 生成的具体字符串实例
- 判断歧义性、生成语言是否为空/有限等

**Lab 5 - Question 3**: 构造CFG：
- 至少包含三个1的字符串：$S \to R1R1R1R$，$R \to 0R \mid 1R \mid \varepsilon$
- 以相同符号开始和结尾：$S \to 1R1 \mid 0R0 \mid 0 \mid 1$
- 奇数长度：$S \to 0S0 \mid 0S1 \mid 1S0 \mid 1S1 \mid 0 \mid 1$
- 奇数长度且中间符号为0：$S \to 0S0 \mid 0S1 \mid 1S0 \mid 1S1 \mid 0$
- 回文：$S \to 0S0 \mid 1S1 \mid 0 \mid 1 \mid \varepsilon$

**Lab 5 - Question 4**: 为上述CFG构造PDA
- 正则语言：PDA不需要使用栈
- 非正则语言（如回文、奇数长度中间为0）：PDA使用栈进行匹配

**Lab 5 - Question 5**: 构造识别复杂语言的CFG：
- 形如 $a^i b^j c^k$ 且满足特定条件的语言

**Lab 5 - Question 6**: 转换为CNF：
- 步骤：添加新起始变元 → 消除 $\varepsilon$-产生式 → 消除单元产生式 → 转换为CNF

---

## Week 6: 下推自动机 (PDA) {#week-6}

### 核心定义

**定义 6.1 (PDA)**: 下推自动机 $P = (Q, \Sigma, \Gamma, \delta, q_0, F)$，其中：
- $\Gamma$ 是栈字母表
- $\delta: Q \times (\Sigma \cup \{\varepsilon\}) \times (\Gamma \cup \{\varepsilon\}) \to \mathcal{P}(Q \times (\Gamma \cup \{\varepsilon\}))$
- 转移形式：$(q, a, b) \to (r, c)$，读入 $a$，弹出 $b$，压入 $c$

**定理 6.1 (PDA 与 CFG 等价)**: 一个语言是上下文无关的，当且仅当某个 PDA 识别它。

**构造方法**:
- **CFG → PDA**: 将起始变元压入栈，通过替换和匹配模拟推导
- **PDA → CFG**: 使用变量 $A_{pq}$ 表示从状态 $p$ 到 $q$ 清空栈的字符串集合

### Lab 问题

**Lab 6 - Question 1**: 为CFG构造PDA：
- 为算术表达式文法 $E \to E+T \mid T$，$T \to T\times F \mid F$，$F \to (E) \mid a$ 构造PDA
- 为复杂CFG构造PDA（带多步压栈路径）

**Lab 6 - Question 2**: PDA转CFG：
- 给定PDA，构造等价CFG
- 基础规则：$A_{pp} \to \varepsilon$
- 连接规则：$A_{pq} \to A_{pr} A_{rq}$
- 匹配规则：对于 push $t$ 和 pop $t$ 的转移对，$A_{pq} \to a A_{rs} b$

**Lab 6 - Question 3**: 证明CFL在并、连接、Kleene Star下封闭

**Lab 6 - Question 4**: 证明每个正则语言都是上下文无关的

**Lab 6 - Question 5**: CNF推导的长度：对于长度为 $|w|$ 的字符串，推导恰好需要 $2|w| - 1$ 步

**Lab 6 - Question 6**: CFL泵引理应用：
- 证明 $\{0^n 1^n 0^n 1^n\}$ 不是CFL
- 证明 $\{0^n \# 0^{2n} \# 0^{3n}\}$ 不是CFL

**Lab 6 - Question 7-8**: CFL不封闭的运算：
- CFL在交运算下不封闭（示例：$A = \{a^m b^n c^n\}$，$B = \{a^n b^n c^m\}$，$A \cap B = \{a^n b^n c^n\}$）
- CFL在补运算下不封闭

**Lab 6 - Question 9**: CFL泵引理与正则语言泵引理的区别

---

## Week 7: 图灵机 (Turing Machine) {#week-7}

### 核心定义

**定义 7.1 (图灵机)**: $M = (Q, \Sigma, \Gamma, \delta, q_0, q_{accept}, q_{reject})$，其中：
- $\Gamma$ 是带字母表，$\sqcup \in \Gamma$ 是空白符
- $\Sigma \subseteq \Gamma \setminus \{\sqcup\}$ 是输入字母表
- $\delta: Q \times \Gamma \to Q \times \Gamma \times \{L, R\}$ 是转移函数
- 图灵机永不停机（对于识别器）或必须停机（对于判定器）

**定义 7.2 (配置/瞬时描述)**: 形如 $u q v$，表示带内容为 $uv$，状态为 $q$，读写头在 $v$ 的第一个符号上。

**定义 7.3 (多带图灵机)**: 具有多个独立读写带的图灵机，等价于单带图灵机。

**定理 7.1**: 以下变体与标准图灵机等价：
- 多带图灵机
- 左移重置图灵机（Left Reset）
- 非确定性图灵机 (NTM)

**定理 7.2**: 只读图灵机（输入只读）等价于 DFA。

**定理 7.3**: 只能使用 $R$（右移）和 $S$（停留）的图灵机恰好识别正则语言。

### Lab 问题

**Lab 7 - Question 1**: 构造双栈PDA识别 $\{a^n b^n c^n \mid n \geq 0\}$：
- 一个栈存 $a$，一个栈存 $b$，然后与 $c$ 匹配

**Lab 7 - Question 2**: 图灵机配置序列：
- 给定转移函数，写出特定输入的配置历史
- 例如：$q_1 0 \vdash \sqcup q_2 0 \vdash \sqcup x q_3 \vdash \ldots$

**Lab 7 - Question 3**: 图灵机设计基础问答

**Lab 7 - Question 4**: 设计识别 $\{a^n b^n c^n\}$ 的图灵机：
- 扫描匹配 $a, b, c$，用 $X, Y, Z$ 标记已计数符号

**Lab 7 - Question 5**: 设计图灵机：
- 识别 $\{w \mid \#0 = \#1\}$：逐对取消 $0$ 和 $1$
- 识别 $\{w \mid \#0 = 2 \cdot \#1\}$：每取消一个 $1$，取消两个 $0$
- 补集：利用可判定语言在补运算下封闭

**Lab 7 - Question 6**: 证明左移重置图灵机与标准图灵机等价：
- 使用扩展字母表 $\Gamma \times \{0, 1, 2\}$ 和辅助状态模拟左移

**Lab 7 - Question 7**: 证明只能 $R/S$ 的图灵机识别正则语言：
- 构造等价DFA，每个状态编码"从当前格右移后的目标状态"

**Lab 7 - Question 8**: 非确定性图灵机封闭性：
- 并、连接、Kleene Star、补、交

**Lab 7 - Question 9-10**: 图灵机算法设计：
- 二进制减一
- 二进制减法 $x - y$
- 整数除法 $\lfloor x/y \rfloor$

**附加: Question 11 (Markdown文件)**: 输入只读图灵机等价于DFA的完整证明

---

## Week 8: 可判定性 (Decidability) {#week-8}

### 核心定义

**定义 8.1 (可判定语言)**: 语言 $L$ 是可判定的，若存在图灵机 $M$ 使得 $M$ 在所有输入上停机，且 $L(M) = L$。

**定义 8.2 (图灵可识别语言)**: 语言 $L$ 是图灵可识别的，若存在图灵机 $M$ 使得 $L(M) = L$（$M$ 可能在非 $L$ 输入上循环）。

**核心可判定语言**:
- $A_{DFA} = \{\langle M, w \rangle \mid M \text{ 是DFA且接受 } w\}$ **可判定**
- $E_{DFA} = \{\langle M \rangle \mid M \text{ 是DFA且 } L(M) = \emptyset\}$ **可判定**
- $EQ_{DFA} = \{\langle M_1, M_2 \rangle \mid M_1, M_2 \text{ 是DFA且 } L(M_1) = L(M_2)\}$ **可判定**
- $A_{CFG}$ **可判定**（使用CYK算法）
- $E_{CFG}$ **可判定**

**核心不可判定语言**:
- $A_{TM} = \{\langle M, w \rangle \mid M \text{ 是TM且接受 } w\}$ **不可判定**（但图灵可识别）
- $HALT_{TM}$ **不可判定**
- $E_{TM}$ **不可判定**
- $EQ_{TM}$ **不可判定**

**定理 8.1 (Rice 定理)**: 任何关于图灵机语言的非平凡性质都是不可判定的。

### Lab 问题

**Lab 8 - Question 1**: 判断给定图灵机在不同输入上的行为

**Lab 8 - Question 2**: 证明 $L = \{\langle M, R \rangle \mid L(M) = L(R)\}$ 可判定：
- 将正则表达式 $R$ 转为 NFA，再转为 DFA $M'$
- 运行 $EQ_{DFA}$ 判定器于 $\langle M, M' \rangle$

**Lab 8 - Question 3**: 证明 $ALL_{DFA}$ 可判定：
- 构造 $\overline{L(A)}$ 的DFA $M$，运行 $E_{DFA}$ 判定器

**Lab 8 - Question 4**: 证明 $A_{\varepsilon\text{-CFG}}$ 可判定

**Lab 8 - Question 5**: 构造图灵机识别 $\overline{E_{TM}}$：
- 枚举所有字符串并模拟，若存在被接受的则接受

**Lab 8 - Question 6**: $EQ_{DFA}$ 判定器的输入长度上界：
- 只需测试长度小于 $n_1 n_2$ 的输入
- 测试串总数：$\sum_{i=0}^{n_1 n_2 - 1} |\Sigma|^i$

---

## Week 9: 可计算性基础 {#week-9}

### 核心定义

**定义 9.1 (函数性质)**: 函数 $f: A \to B$：
- **单射 (Injective/One-to-one)**: $f(a) = f(b) \Rightarrow a = b$
- **满射 (Surjective/Onto)**: $\forall b \in B, \exists a \in A, f(a) = b$
- **双射 (Bijective)**: 既是单射又是满射

**定义 9.2 (可数/不可数)**: 集合 $S$ 是**可数的**，若存在从 $\mathbb{N}$ 到 $S$ 的双射；否则为**不可数的**。

**定理 9.1 (Cantor 对角线论证)**: 实数集 $\mathbb{R}$ 是不可数的。$

**定理 9.2**: $\{0, 1, 2\}^\mathbb{N}$（所有三元序列）是不可数的。

**定理 9.3**: 所有图灵机是可数的（因为每个TM可用有限字符串编码），但所有语言是不可数的，因此存在不可计算的语言。

### Lab 问题

**Lab 9 - Question 1**: 判断函数性质：单射、满射、双射

**Lab 9 - Question 2**: 使用对角线论证证明 $\{0,1,2\}^\mathbb{N}$ 不可数

**Lab 9 - Question 3**: 证明三元组集合 $O = \{(i,j,k) \mid i,j,k \in \mathbb{N}\}$ 是可数的

**Lab 9 - Question 4**: 判定图灵机是否会在带上写符号：
- 只需模拟 $|S|+1$ 步，若仍未写符号则永远不会写

**Lab 9 - Question 5**: 判定图灵机状态数是否 $\geq 481$：
- 解码输入并计数状态数即可

**Lab 9 - Question 6**: 判定DFA是否接受任何具有偶数个1的字符串：
- 构造DFA $A$ 接受偶数个1的字符串
- 计算 $M \cap A$ 并判定 $L(M \cap A) = \emptyset$

---

## Week 10: 归约与复杂性入门 {#week-10}

### 核心定义

**定义 10.1 (映射归约)**: $A \leq_m B$（$A$ 可映射归约到 $B$），若存在可计算函数 $f$ 使得 $w \in A \iff f(w) \in B$。

**性质**: 若 $A \leq_m B$ 且 $B$ 可判定，则 $A$ 可判定；若 $A \leq_m B$ 且 $B$ 图灵可识别，则 $A$ 图灵可识别。

**定义 10.2 (co-Turing-recognizable)**: $A$ 是 co-Turing-可识别的，若 $\overline{A}$ 是图灵可识别的。

**定理 10.1**: $A$ 可判定 $\iff$ $A$ 图灵可识别且 co-Turing-可识别。

**定理 10.2 (Rice 定理应用)**: 任何关于TM语言的非平凡性质都是不可判定的。

**定义 10.3 (P类)**: $P$ 是所有可被确定性图灵机在多项式时间内判定的语言的集合。

**性质**: $P$ 在并、连接、补运算下封闭。

### Lab 问题

**Lab 10 - Question 1**: 证明 $\overline{EQ_{CFG}}$ 是图灵可识别的：
- 枚举所有字符串，在 $G_1$ 和 $G_2$ 上并行运行 $A_{CFG}$
- 若一个接受而另一个拒绝，则接受

**Lab 10 - Question 2**: 归约保持正则性的反例：
- $A = \{1^k 0^k\}$ 非正则，$B = \{1\}$ 正则，但 $A \leq_m B$

**Lab 10 - Question 3**: 若 $A$ 图灵可识别且 $A \leq_m \overline{A}$，则 $A$ 可判定

**Lab 10 - Question 4-6**: 各种归约构造

**Lab 10 - Question 7**: Rice 定理应用：
- "$L(M)$ 是无限的" — 不可判定
- "$1011 \in L(M)$" — 不可判定
- "$L(M) = \Sigma^*$" — 不可判定

**Lab 10 - Question 8**: 判断真假（关于可判定性、可识别性）

**Lab 10 - Question 9**: CYK 算法分析表

**Lab 10 - Question 10-13**: $P$ 类问题：
- $P$ 在并、连接、补下封闭
- $CONNECTED$（图连通性）$\in P$（BFS）
- $ALL_{DFA} \in P$（BFS检查补图是否可达接受状态）
- $EQ_{DFA} \in P$（对称差 + $E_{DFA}$）

---

## Week 11: NP 类问题 {#week-11}

### 核心定义

**定义 11.1 (NP)**: 语言 $L \in NP$ 当且仅当存在多项式时间验证器 $V$ 使得：
- $w \in L \Rightarrow \exists c,\ |c| \leq |w|^k,\ V(w, c) \text{ 接受}$
- $w \notin L \Rightarrow \forall c,\ V(w, c) \text{ 拒绝}$

**核心NP问题**:
- 3-SAT（3-CNF 可满足性）
- VERTEX-COVER（顶点覆盖）
- TSP（旅行商问题）
- MAX-CUT（最大割）
- 3-COLORING（3着色）

**性质**:
- $P \subseteq NP$
- $P$ 在补运算下封闭
- 若 $P = NP$，则 $NP = coNP$

### Lab 问题

**Lab 11 - Question 1**: 证明以下问题属于 NP：
- 3-SAT：证书为赋值 $x$，验证器检查每个子句
- VERTEX-COVER：证书为 $k$ 个顶点，验证器检查每条边
- TSP：证书为路径，验证器检查访问每个城市且总距离 $\leq$ 阈值
- MAX-CUT：证书为割，验证器检查交叉边数
- 3-COLORING：证书为着色方案，验证器检查相邻顶点不同色

**Lab 11 - Question 2**: 证明 NP 在并、连接、Kleene Star 下封闭

**Lab 11 - Question 3**: 判断真假：
- (a) $P \subseteq NP$ — **True**
- (b) $P$ 在补运算下封闭 — **True**
- (c) $\overline{SAT} \in P$ — **False**（若真则 $P = NP$）
- (d) $P \subseteq NP \cap coNP$ — **True**
- (e) 若 $P = NP$ 则 $NP = coNP$ — **True**

**Lab 11 - Question 4-5**: 2-SAT 和 2-COLOR：
- 2-SAT $\in P$（蕴含图 + SCC 检测）
- 2-COLOR $\in P$（BFS 二分图检测）

---

## Week 12: NP 完全性 {#week-12}

### 核心定义

**定义 12.1 (NP-完全)**: 语言 $B$ 是 NP-完全的，若：
1. $B \in NP$
2. $\forall A \in NP,\ A \leq_p B$（多项式时间归约）

**核心NP-完全问题**:
- 3-SAT（第一个被证明的 NP-完全问题）
- CLIQUE（团问题）
- VERTEX-COVER
- HAMPATH（哈密顿路径）
- SUBSET-SUM

**归约方法**:
- 从 3-SAT 归约到目标问题
- 构造变量装置（variable gadget）和子句装置（clause gadget）

### Lab 问题

**Lab 12 - Question 1**: 3-SAT 到 3-CLIQUE 的归约：
- 为每个子句中的每个文字出现创建一个顶点
- 若两个顶点来自不同子句且不矛盾，则连边
- 目标团大小 $k = 3$（子句数）
- 每个3-团对应一个满足赋值

**Lab 12 - Question 2**: 2-SAT 到 CLIQUE 的归约：
- 类似构造，目标团大小 $k = m$（子句数）
- 注意：2-SAT $\in P$，因此这仅证明 $CLIQUE$ 是 NP-hard 的辅助步骤

**Lab 12 - Question 3**: 分析给定 2-CNF 的 CLIQUE 归约实例

**Lab 12 - Question 4**: Double-SAT 的 NP-完全性：
- Double-SAT = $\{\phi \mid \phi \text{ 至少有两个满足赋值}\}$
- 构造 $\phi' = \phi \wedge (w \vee \overline{w})$，$\phi \in 3SAT \iff \phi' \in \text{Double-SAT}$

**Lab 12 - Question 5**: 3-SAT 到 HAMPATH 的归约：
- Sipser 风格钻石变量装置
- 水平遍历 = true，垂直遍历 = false
- 子句顶点通过"导线"连接到对应文字位置

---

## Week 13: Cook-Levin 定理与更多归约 {#week-13}

### 核心定理

**定理 13.1 (Cook-Levin 定理)**: SAT 是 NP-完全的。即，任何 NP 问题都可以多项式时间归约到布尔可满足性问题。

**构造方法 (Tableau/计算历史)**: 
- 将图灵机 $M$ 在输入 $w$ 上的计算表示为 $n \times n$ 的表格（tableau）
- 每行代表一个配置
- 用布尔变量 $x_{i,j,s}$ 表示单元格 $(i,j)$ 包含符号 $s$

**合法性约束**:
- **起始条件** ($\phi_{start}$)：第一行正确编码初始配置
- **接受条件** ($\phi_{accept}$)：某处出现 $q_{accept}$
- **转移合法性** ($\phi_{move}$)：每个 $2 \times 3$ 窗口符合转移函数

**关键发现**: 仅检查 $2 \times 2$ 窗口**不够**，因为单个转移可能影响3列。

### Lab 问题

**Lab 13 - Question 1**: 图灵机计算历史表：
- 为 $M$ 在 $w=111$ 上的计算构造 $5 \times 5$ 表
- 初始配置：$q_0 111\sqcup \vdash 1 q_1 11\sqcup \vdash \ldots$
- 定义布尔变量 $x_{i,j,s}$
- 构造 $\phi_{start}$ 和 $\phi_{accept}$

**Lab 13 - Question 2**: 判断给定 $2 \times 3$ 窗口的合法性

**Lab 13 - Question 3**: 证明 $2 \times 2$ 窗口不足够：
- 构造反例：转移 $\delta(q,0)=(r,0,L)$，$\delta(q,1)=(s,1,L)$
- 存在非法 $2 \times 3$ 窗口，但其两个 $2 \times 2$ 子窗口都合法

**Lab 13 - Question 4**: 有向 HAMPATH 到无向 UHAMPATH 的归约：
- 每个顶点 $u$ 替换为三个顶点 $u^{in}, u^{mid}, u^{out}$
- 添加内部边 $\{u^{in}, u^{mid}\},\{u^{mid}, u^{out}\}$
- 每条有向边 $(u,v)$ 变为 $\{u^{out}, v^{in}\}$
- 起始 $s' = s^{in}$，终点 $t' = t^{out}$

**Lab 13 - Question 5**: 3-SAT 到 VERTEX-COVER 的归约：
- **变量装置**：每个变量 $x_i$ 创建 $x_i$ 和 $\overline{x_i}$ 及边 $\{x_i, \overline{x_i}\}$
- **子句装置**：每个子句创建三角形 $\{\ell_1, \ell_2\},\{\ell_2, \ell_3\},\{\ell_3, \ell_1\}$
- **连接**：子句文字顶点连接到变量装置中同名顶点
- 目标覆盖大小：$k = v + 2c$

---

## 附录：核心定理汇总

### 正则语言
| 定理 | 内容 |
|------|------|
| Kleene 定理 | 正则语言 $\iff$ DFA $\iff$ NFA $\iff$ 正则表达式 |
| 泵引理 | 正则语言可被"泵送"，用于证明非正则性 |
| 封闭性 | 并、交、补、连接、Kleene Star 下封闭 |
| Myhill-Nerode | 状态数等于等价类数 |

### 上下文无关语言
| 定理 | 内容 |
|------|------|
| CFG-PDA 等价 | CFL $\iff$ PDA |
| 泵引理 (CFL) | $s = uvxyz$，$|vxy| \leq p$，$|vy| \geq 1$，$uv^ixy^iz \in L$ |
| 封闭性 | 并、连接、Kleene Star 封闭；交、补不封闭 |
| CNF | 任何CFG可转换为乔姆斯基范式 |

### 图灵机与可计算性
| 定理 | 内容 |
|------|------|
| Church-Turing 论题 | 直观算法 = 图灵机 |
| 判定 vs 识别 | 可判定 = 总是停机；可识别 = 接受时停机 |
| Rice 定理 | 任何非平凡语言性质不可判定 |
| 可数性 | 所有图灵机可数，所有语言不可数，存在不可计算语言 |

### 复杂性
| 定理/概念 | 内容 |
|-----------|------|
| $P \subseteq NP$ | 多项式时间可判定的都可验证 |
| Cook-Levin | SAT 是 NP-完全的 |
| NP-完全归约 | 3-SAT $\leq_p$ CLIQUE $\leq_p$ VERTEX-COVER $\leq_p$ HAMPATH 等 |
| $P = NP$? | 未解决的千禧年大奖难题 |

---

*报告生成时间: 2026-06-12*  
*数据来源: C:\Users\edwar\Introduction-to-Theory-of-Computation-SUSTech-2026-Spring*
